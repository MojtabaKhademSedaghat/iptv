# Disabled on this fork.
# Upstream iptv-org update workflow needs private app secrets and deploys to iptv-org repos.
# Iran playlist sync is handled by sync-iran.yml instead.
name: update
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Upstream update workflow is disabled on this Iran-only fork."
